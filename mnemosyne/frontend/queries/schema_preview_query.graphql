query SchemaPreviewQuery($id: Uuid!) {
    schema(id: $id) {
        id,
        name,
        queryAddress,
        topic,
        type,
        definition(version: "*") {
            version,
            definition
        }
    }
}
